apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: timescaledb-data
  name: timescaledb-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
